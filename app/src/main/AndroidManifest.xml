<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mit.persona">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Persona"></activity>
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Event" />
        <activity
            android:name=".DatabaseAll"
            android:label="@string/title_activity_database_all"
            android:theme="@style/AppTheme" />
        <activity android:name=".Event" />
        <activity
            android:name=".testMongo"
            android:label="@string/title_activity_test_mongo"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>